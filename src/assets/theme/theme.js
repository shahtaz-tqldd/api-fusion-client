import { createTheme } from "@mui/material";


const theme = createTheme({
  palette: {
    primary: {
      main: '#007bff', // Change this to your desired color
    },
    secondary: {
      main: '#ff9800', // Change this to your desired color
    },
  },
});

export default theme;
